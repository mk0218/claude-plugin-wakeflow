<!-- Shared pointer (SSOT): tells the subcommand to consult the full workflow definition when needed.
     Injected by every task/issue subcommand via !`cat ${CLAUDE_PLUGIN_ROOT}/rules/wakeflow-ref.md` at entry. -->

For concepts, directory layout, relationships, and operating principles, read
`${CLAUDE_PLUGIN_ROOT}/reference/wakeflow.md` first — skip if it's already in context.
